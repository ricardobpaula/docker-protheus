# Detalhes de build

- Criar build ```docker build -t {username}/{container_name} ./{container_folder}```

- Criar tag ```docker image tag {username}/{container_name} {username}/{container_name}:{version}```

- Publicar ```docker push {username}/{container_name}:{version}```